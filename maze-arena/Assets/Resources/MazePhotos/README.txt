Place textures here (Resources/MazePhotos) to paint corridor photo frames.
Filenames should match catalog ids (monalisa.png, scream.png, …).
Copies of Assets/Game/Memory/Textures/Photos are synced here so both memory walls
and the package painter can load real photos instead of color swatches.
