# Legacy dance-floor snapshot

Frozen copy of the original `TileGlow.shader` (`TileGlow.Legacy.shader`).

The live coloured glass dance floor remains in `../DanceFloor*.cs` and is still the default. C# sources are not duplicated here because Unity would compile two copies of the same types.
